# Backend Environment Variables

# MongoDB Connection String
# Get this from MongoDB Atlas (free M0 cluster at https://www.mongodb.com/cloud/atlas)
# Example: mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/regle-de-trois?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/regle-de-trois

# JWT Secret for authentication tokens
# Generate a strong random string (min 32 characters recommended)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=f0c739d602cb618eb2ca646ef8d3e77ac6e84a83f9e168c0b7c815f62d487d53

# Port to run the server on (optional, defaults to 3001)
PORT=3001

# Frontend URL (optional, for CORS configuration)
FRONTEND_URL=http://localhost:5173
